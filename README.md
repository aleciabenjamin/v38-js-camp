**1) Peka på en const och en let — varför just den typen där?**\
const isMember - Det är en variabel som inte kommer att ändras.
let shipping - Det är en variabel som kan ändra beroende på huruvida kunden är medlem eller inte.\
**2) Peka på ett anrop med () — vad returneras?**\
console.log("Ordinarie frakt") - en sträng.

**3) Peka på ett === — vad jämförs?**\
(isMember === true) - variabeln och boolean.

**A) Vad är skillnaden mellan deklaration och anrop av en function?**\
En function är ett namngivet kodblock: du deklarerar stegen en gång. Inget körs förrän du anropar med (). return skickar resultatet tillbaka — utan return får du undefined.\
**B) Varför börjar array-index på 0?**\
Array-index börjar på 0 eftersom indexet representerar ett avstånd (offset) från arrayens start-minnesadress.\
**C) Vad betyder undefined när du läser cities[99]?**\
Den existerar inte.
